<p id="notice"><%= notice %></p>

<%= render 'detail' %>

<% if current_user.type == 'Teacher' %>
  <%= link_to 'Back to Previous page', teacher_path(current_user) %>
<% elsif current_user.type == 'Student'%>
  <%= link_to 'Back', :back %>
<% elsif  current_user.type == 'Admin'%>
  <%= link_to 'Back', :back %>
<% end %>
