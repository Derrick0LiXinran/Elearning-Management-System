<%= form_with(model: purchased_history, local: true) do |form| %>
  <% if purchased_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchased_history.errors.count, "error") %> prohibited this purchased_history from being saved:</h2>

      <ul>
        <% purchased_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
